# 📓 แบบฝึกปฏิบัติการ — การวิเคราะห์ข้อมูลและการสร้างแบบจำลองเบื้องต้น

> เอกสารประกอบการฝึกอบรมข้าราชการ/บุคลากรภาครัฐ · ฝึกวิเคราะห์ข้อมูลเปิดภาครัฐด้วย Python บน **Google Colab**
> กรณีศึกษา: ข้อมูลเบาะแส/เรื่องร้องเรียนยาเสพติด สำนักงาน ป.ป.ส. (ONCB)

| รูปแบบ | ภาษา | แพลตฟอร์ม | ระดับ |
|---|---|---|---|
| Notebook สอน + แบบฝึกปฏิบัติ (มีเฉลย) | ไทย (ทางการ) | Google Colab | พื้นฐาน → สูง |

---

## 🎯 สิ่งที่ผู้เข้ารับการอบรมจะได้ฝึก

| ส่วน | หัวข้อ | ประเภท |
|---|---|---|
| 0 | ทำความรู้จักข้อมูล + คำสั่งพื้นฐาน pandas (`head` · `info` · `isna` · `dropna` · `describe`) | 🧑‍🏫 บรรยาย |
| 1 | บทเรียนการนำเสนอข้อมูล — Histogram → Bar → Line → แผนที่ (Choropleth + GeoJSON) | 🧑‍🏫 บรรยาย |
| 2 | แบบฝึกปฏิบัติการนำเสนอข้อมูล (ข้อ 1–5) | ✍️ ปฏิบัติ |
| 3 | บทเรียนการสร้างแบบจำลอง — Clustering (K-Means + PCA) และ Regression (Linear) | 🧑‍🏫 บรรยาย |
| 4 | แบบฝึกปฏิบัติการสร้างแบบจำลอง (ข้อ 6–7) | ✍️ ปฏิบัติ |

**รูปแบบแบบฝึกปฏิบัติ:** แต่ละข้อมีตัวอย่างเริ่มต้นในเซลล์โค้ด → มีกล่อง **แนวโค้ด** (โครงสำหรับเติมช่องว่าง `____`) → และกล่อง **เฉลย** (โค้ดฉบับสมบูรณ์)

---

## ▶️ วิธีเริ่มใช้งาน

1. เปิดไฟล์ [`DGA_Assignment.ipynb`](DGA_Assignment.ipynb) ใน **Google Colab**
2. รัน 3 เซลล์แรก (ติดตั้งไลบรารี · ดาวน์โหลดข้อมูล · เรียกใช้ไลบรารี)
3. ทำตามเนื้อหาจากส่วนที่ 0 → 4 ตามลำดับ

> เอกสารนี้ **ดาวน์โหลดข้อมูลเองอัตโนมัติ** ผ่าน `wget` จากโฟลเดอร์ [`ปปส/data/`](../ปปส/data/) — ไม่ต้องอัปโหลดไฟล์ กด *Run all* ได้ทันที

---

## 📊 ข้อมูลที่ใช้

| ไฟล์ | เนื้อหา |
|---|---|
| `oncb_verify_monthly.csv` | เบาะแสยาเสพติดรายเดือน 8,669 แถว · 77 จังหวัด · พ.ศ. 2560–2569 |
| `th_provinces.geojson` | ขอบเขต 77 จังหวัด (สำหรับแผนที่ Choropleth) |
| `th_province_population.csv` | จำนวนประชากรรายจังหวัด (สำหรับการวิเคราะห์ per-capita) |

ที่มา: สำนักงาน ป.ป.ส. (ONCB) — ดูรายละเอียด API และพจนานุกรมข้อมูลได้ที่ [`ปปส/README.md`](../ปปส/README.md)

---

## 🧰 เนื้อหาด้านเทคนิค

| หมวด | เครื่องมือ |
|---|---|
| จัดการข้อมูล | **pandas** (`read_csv` · `groupby` · `merge` · `dropna`) |
| นำเสนอข้อมูล | **Plotly Express** — histogram · bar · line · choropleth |
| การจัดกลุ่ม | **scikit-learn** — StandardScaler → KMeans → PCA |
| การถดถอย | **scikit-learn** — LinearRegression (R² · MAE · การทำนายค่าใหม่) |

---

## 📁 ไฟล์

```
Assignment รุ่นที่ 3/
├── README.md
└── DGA_Assignment.ipynb    ← notebook สอน + แบบฝึกปฏิบัติ (74 เซลล์)
```
